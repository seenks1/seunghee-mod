module.exports = {
	run: function (client, message, args) {
		message.channel.send("If the bot is offline, contact 1gnite#1130 to find out when the bot comes back up again.");
	},

	help: {
		name: "botisdead"
	}
};
